{
  "name": "niba-classic",
  "version": "1.1.0",
  "scripts": {
    "prepare": "npx cap add ios && npx @trapezedev/configure run capawesome.yml -y --ios-project ios/App"
  },
  "dependencies": {
    "@capacitor/cli": "^4.8.2",
    "@capacitor/core": "^4.8.2",
    "@capacitor/ios": "^4.8.2"
  },
  "devDependencies": {
    "@trapezedev/configure": "^7.0.0"
  }
}